import { Meta, Story, Preview, ArgsTable } from '@storybook/addon-docs/blocks';
import { text, withKnobs } from '@storybook/addon-knobs';
import { faSearch } from '@fortawesome/free-solid-svg-icons';

import Form from 'src/Form';
import FormGroup from 'src/FormGroup';
import CheckboxButtonGroup from 'src/CheckboxButtonGroup';
import CheckboxButton from 'src/CheckboxButton';
import FormControlLabel from 'src/FormControlLabel';
import Input from 'src/Input';
import InputLabel from 'src/InputLabel';
import { InputComponent, CheckboxButtonGroupComponent } from './FormGroup.stories'

<Meta title="Design System/Input Fields" component={FormGroup} />

# Input Fields Anatomy

Input fields let users enter and edit text. These are the variants available in the design system.

## Placeholder Text

- Used to give the user an idea of what sort text they should include here. Does not carry over as default text if no other text is added, so if left blank will produce an error. 

<Preview>
<Story name="Input Fields">
<FormGroup id="default">
  <InputComponent id="default-input" name="default" placeholder="Placeholder text" />
</FormGroup>
</Story>
</Preview>


## Helper Text

- Information tied to a field that gives suggestions or additional information that might be beneficial to the user

<Preview>
<Story name="With Label">
<FormGroup
    id="with-label"
    label="Label"
    helperText="test helper text"
    labelHtmlFor="input"
  >
    <InputComponent id="input" name="with-label" placeholder="Placeholder text" />
  </FormGroup>
</Story>
</Preview>

## Required

- To indicate that a field is required, display "(Required)" next to the label
- If some fields are required, indicate all required ones
- If most fields are required, indicate optional fields by displaying "(Optional)" next to the label

<Preview>
<Story name="Required">
<FormGroup
id="with-required"
label="Label"
required
>
<InputComponent id="with-required-input" name="required" placeholder="Placeholder text" />
</FormGroup>
</Story>
</Preview>

## With Tooltip

- Information users will typically only reference once (and then it's learned) or here and there. This information is typically more of a recommendation, and not vital for the user to know, but helpful if they do!

<Preview>
<Story name="With Tooltip">
<FormGroup
    id="with-label"
    label="Label"
    labelHtmlFor="input"
    labelTooltip={text('Tooltip', 'Some tooltip text')}
  >
    <InputComponent id="input" name="with-label" placeholder="Placeholder text" />
  </FormGroup>
  </Story>
  </Preview>

## Leading Icon

<Preview>
<Story name="Leading Icon">
  <FormGroup
    id="with-leading-icon"
    labelHtmlFor="input"
  >
    <InputComponent id="input" leadingIcon={faSearch} name="with-leading-icon" placeholder="Placeholder text" />
  </FormGroup>
</Story>
</Preview>

## Trailing Icon

<Preview>
<Story name="Trailing Icon">
<FormGroup
    id="with-trailing-icon"
    labelHtmlFor="input"
  >
    <InputComponent id="input" name="with-leading-icon" trailingIcon={faSearch} placeholder="Placeholder text" />
  </FormGroup>
  </Story>
  </Preview>

## Leading & Trailing Icons

<Preview>
<Story name="Leading and Trailing Icons">
  <FormGroup
    id="with-trailing-icon"
    labelHtmlFor="input"
  >
    <InputComponent id="input" leadingIcon={faSearch} name="with-leading-and-trailing-icons" trailingIcon={faSearch} placeholder="Placeholder text" />
  </FormGroup>
    </Story>
    </Preview>

## Error

- Keep the text as short as possible; for complex info, provide a link to more information in a new browser tab

<Preview>
<Story name="Error">
  <FormGroup
    displayErrorText
    errors={{ test: [<div>Sorry, we’re not able to accept this type of input. For more information, click <a href="http://www.google.com">here</a></div>] }}
    id="with-errors"
    inputKey="test"
    label="Label"
    labelHtmlFor="input"
  >
    <InputComponent id="input" name="with-errors" placeholder="Placeholder text" />
  </FormGroup>
</Story>
</Preview>