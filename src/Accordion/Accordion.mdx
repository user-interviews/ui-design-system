import { ArgTypes, Canvas } from '@storybook/blocks';
import Accordion from './Accordion';
import * as ComponentStories from './Accordion.stories';

# Accordion

##

<h4>
  Vertically-stacked header that reveal or hide additional content.
</h4>

<Canvas of={ComponentStories.Default} />

### When to use

### When not to use


## Props

<ArgTypes of={Accordion} />

## Stories

### Default

<Canvas of={ComponentStories.Default} />

### Default Open

<Canvas of={ComponentStories.DefaultOpen} />

### Chevron Left

<Canvas of={ComponentStories.ChevronLeft} />

### Borderless

<Canvas of={ComponentStories.Borderless} />

### In Card
When multiple Cards are stacked, you want to ensure there is proper content alignment with padding. When wrapping `Accordion` in `Card`:
1. Use `<Card noPadding>`
2. Use `<AccordionItem borderless>`

<Canvas of={ComponentStories.InCard} />

### Separate

<Canvas of={ComponentStories.Separate} />

### Disabled

<Canvas of={ComponentStories.Disabled} />
