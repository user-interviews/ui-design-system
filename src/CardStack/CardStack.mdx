import { ArgTypes, Canvas } from '@storybook/addon-docs/blocks';
import CardStack from './CardStack';
import * as ComponentStories from './CardStack.stories';

# CardStack

##

<h4>
	A simple layout container for positioning Cards.
</h4>

<Canvas of={ComponentStories.Default} />

### When to use 
- Use `CardStack` to wrap one or more `Card` components.

## Props

<ArgTypes of={CardStack} />

## Stories

### Default 

<Canvas of={ComponentStories.Default} />

### Centered

<Canvas of={ComponentStories.Centered} />
